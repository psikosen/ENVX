# Core
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
PyYAML>=6.0,<7.0

# envx pipeline
jsonschema>=4.21.0,<5.0.0
rapidfuzz>=3.6.0,<4.0.0
httpx>=0.27.0,<1.0.0
cryptography>=41.0.0
cffi>=1.16.0

# Optional: cold-tier vector store (~97% storage reduction).
# Without it the cold tier degrades to a null store.
# leann>=0.1.0

# Optional: applying DDL migrations.
# psycopg[binary]>=3.1

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Dev tools
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
